apiVersion: v1
kind: ConfigMap
metadata:
  name: django-config
  namespace: default
data:
  DEBUG: "True"
  DB_HOST: "postgres-service"
  DB_PORT: "5432"
  DJANGO_ALLOWED_HOSTS: "localhost,127.0.0.1,minikube.local"
  ENVIRONMENT: "production"
